[package]
name = "ai_workflows"
version = "0.1.0"
edition = "2024"
license = "Proprietary"
publish = false

[dependencies]
bim_model = { path = "../bim_model" }

[dev-dependencies]
geometry_kernel = { path = "../geometry_kernel" }